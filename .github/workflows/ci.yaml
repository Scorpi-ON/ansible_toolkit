---
name: Ansible Lint

on:
  push:
    branches: ["main"]
    paths-ignore:
      - "README.md"
      - "LICENSE"
      - ".gitignore"
      - "pyproject.toml"
      - "uv.lock"
      - "*.example"
      - ".github/"
  pull_request:
    branches: ["main"]
    paths-ignore:
      - "README.md"
      - "LICENSE"
      - ".gitignore"
      - "pyproject.toml"
      - "uv.lock"
      - "*.example"
      - ".github/"
permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Install lint dependencies
        run: uv sync --frozen

      - name: Install Ansible dependencies
        run: uv run -m ansible-galaxy install -r requirements.yml

      - name: Run ansible-lint
        run: uv run -m ansible-lint
